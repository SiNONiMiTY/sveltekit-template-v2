import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.eightcoretech.netizenwatch',
    appName: 'Netizen Watch',
    webDir: 'build',
    bundledWebRuntime: false,
}

export default config
